añadir lista desplegable el crear nuevo hotel que permita decidir el lugar

termianr resrvar hotel

añadir un como llegar en recomendaciones

añadir ver en el mapa página hotel

Lista desplegable"DropdownButtonFormField<String>(
                          value: _customer_uidController.text.isNotEmpty
                              ? _customer_uidController.text
                              : null,
                          items: model.customers
                              .map<DropdownMenuItem<String>>((customer) {
                            return DropdownMenuItem<String>(
                              value: customer['uid'].toString(),
                              child: Text(customer['uid'].toString()),
                            );
                          }).toList(),
                          onChanged: (value) {
                            setState(() {
                              _customer_uidController.text = value ?? '';
                            });
                          },
                          decoration:
                              const InputDecoration(labelText: 'Customer'),
                          validator: (value) {
                            if (value == null || value.isEmpty) {
                              return 'Por favor, seleccione un customer';
                            }
                            return null;
                          },
                        )"